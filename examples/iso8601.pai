#[[
@brief Get the current date and time in ISO-8601 format.
@return string an ISO formatted datetime string.
#]]
function iso_time
	return "{${sysdate}.date()}T{${systime}.floor().time()}"
end

print ${call iso_time}
 